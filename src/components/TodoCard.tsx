import React from "react";

/**
 * Design Todo Card which accepts the props like taskName, taskDescription, date and time, section .
 * @returns
 */
const TodoCard = () => {
  return <div>Todo Card</div>;
};

export default TodoCard;
